<h3> All Students </h3>

<% @students.each do |student| %>
<%= link_to student.name, student_path(student) %>
<h6><%= link_to "delete", student_path(student), method: :delete %></h6><br>
<%end%>

<%= link_to "Create a New Student", new_student_path %>
