<h3> Create a New Student </h3>

<%= form_for @student do |f| %>
   <%= f.label :name %>
   <%= f.text_field :name %>

   <%= f.submit %>

   <% end %>

   <%= link_to "Back to All Students", students_path %>
