<h3> Edit <%= @student.name %>

  <%= form_for @student do |f| %>
     <%= f.label :name %>
     <%= f.text_field :name %>

     <%= f.submit %>

     <% end %>

<%= link_to "Back to All Students", students_path %>
